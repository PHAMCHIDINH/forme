name: CI

on:
  push:
    branches: ["main", "codex/**"]
  pull_request:
    branches: ["main"]

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: chidinh_client
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: chidinh_client/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build frontend
        run: npm run build

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: chidinh_api
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: goclaw
          POSTGRES_PASSWORD: 05b23c710055bce2147f4f58a84cd252
          POSTGRES_DB: goclaw
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U goclaw -d goclaw"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: chidinh_api/go.mod
          cache-dependency-path: chidinh_api/go.sum

      - name: Run tests
        env:
          DATABASE_URL: postgresql://goclaw:05b23c710055bce2147f4f58a84cd252@127.0.0.1:5432/goclaw?sslmode=disable
          DB_HOST: 127.0.0.1
          DB_PORT: "5432"
          DB_USER: goclaw
          DB_PASSWORD: 05b23c710055bce2147f4f58a84cd252
          DB_NAME: goclaw
        run: go test ./...

      - name: Build backend
        run: go build ./cmd/api
